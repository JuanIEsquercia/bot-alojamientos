# Despliegue en Cloud Run

## Pasos

1. El código está en GitHub: `JuanIEsquercia/bot-alojamientos`
2. En Cloud Run, despliega desde GitHub
3. Configura variables de entorno
4. Monta el secreto `firebase-credentials` en `/var/www/html/firebase-credentials.json`

## URL del webhook

`https://tu-url.run.app/webhook.php`

